[tool.ruff]
line-length = 100
target-version = "py311"
src = ["code"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
pythonpath = ["code"]
testpaths = ["code/tests"]
addopts = "--basetemp=.pytest_tmp"
